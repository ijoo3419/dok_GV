<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Member">
	<resultMap type="Member" id="memberResultSet">
		<id property="mid" column="MID"/>
		<result property="userId" column="EMAIL"/>
		<result property="userPwd" column="USER_PWD"/>
		<result property="userName" column="USER_NAME"/>
		<result property="birthDay" column="BIRTHDAY"/>
		<result property="gender" column="GENDER"/>
		<result property="phone" column="PHONE"/>
		<result property="address" column="USER_CLASS"/>
		<result property="enrollDate" column="STATUS"/>
		<result property="modifyDate" column="LOGIN_CLASS"/>
		<result property="status" column="BLACKLIST_STATUS"/>
		<result property="modifyDate" column="JOIN_DATE"/>
		<result property="status" column="WITHDRAW_DATE"/>
		<result property="modifyDate" column="NICKNAME"/>
	</resultMap>
	 <!--  로그인 정보 조회용 쿼리문  -->
	<select id="loginCheck" resultMap="memberResultSet" parameterType="Member">
		SELECT * 
		FROM MEMBER 
		WHERE USER_ID = #{userId} 
		AND USER_PWD = #{userPwd} 
		AND STATUS = 'Y'
	</select>
	
	<!-- 회원 가입용 메소드 -->
	<insert id="insertMember" parameterType="Member">
		INSERT INTO MEMBER
		VALUES(SEQ_MID.NEXTVAL, #{userId}, #{userPwd}, #{userName}, 
			   #{email}, #{birthDay}, #{gender}, #{phone}, #{address}, 
			   SYSDATE, SYSDATE, DEFAULT)
	</insert>
	
	<!-- 암호화된 비밀번호 조회용 메소드 -->
	<select id="selectPwd" resultType="java.lang.String" parameterType="java.lang.String">
		SELECT USER_PWD
		FROM MEMBER
		WHERE USER_ID = #{userId}
		AND STATUS = 'Y'
	</select>
	
	<!-- 비밀번호 일치 시 회원 정보 조회용 쿼리문 -->
	<select id="selectLoginUser" resultMap="memberResultSet" parameterType="Member">
		SELECT *
		FROM MEMBER
		WHERE USER_ID = #{userId}
		AND STATUS = 'Y'
	</select>
	
	<insert id="insertBoard">
		INSERT INTO BOARD(BID, BTITLE)
		VALUES(SQE_BID.NEXTVAL, '새로운 회원이 가입되었습니다.')
	</insert>
</mapper>